{
  "name": "9remote",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "package"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=package",
    "build": "npm run build --workspace=package && rm -rf dist && cp -r package/dist dist",
    "lint": "npm run lint --workspace=package",
    "start": "node dist/server.cjs"
  }
}
